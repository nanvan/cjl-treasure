
电商项目的表:
	冗余:品牌(对于一两年不会更改名字的)里加上类别字段,不要再去根据id查表(性能低),缺点就是多占用点
硬盘空间
	表加上字段:添加时间,修改时间
	不用建立外键,但是会有外键关系

表的设计结构的三大范式:
	1NF : 原子性,字段不可分
	2NF : 唯一性,一个表之说明一个事物
	3NF : 每列都与主键有直接关系,不存在传递依赖(即其他表的)

ERP : 传统管理系统

典型的性能优化方式:
	用空间换时间(即用磁盘空间(冗余)换取查询速度)

什么数据可以用来做缓存:冗余的数据
能够冗余的数据一般都是不易变的